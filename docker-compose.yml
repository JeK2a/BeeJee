version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beejee-web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
